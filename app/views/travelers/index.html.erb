<h1>Travelers#index</h1>
<p>Find me in app/views/travelers/index.html.erb</p>
<% if user_signed_in? %>
	<% if current_user.traveler? %>
		<%= render 'dashboard_traveler' %>
	<% else %>
		Guest
	<% end %>
<% end %>
